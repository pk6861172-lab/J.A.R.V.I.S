
# J.A.R.V.I.S v2.0 — Requirements
# Install all: pip install -r requirements.txt

# Core AI (OpenRouter)
openai>=1.0.0

# .env support (keeps API keys secure)
python-dotenv>=1.0.0

# Voice Input
SpeechRecognition>=3.10.0
PyAudio>=0.2.14          # if this fails: pipwin install pyaudio

# Voice Output
pyttsx3>=2.90

# System Monitoring
psutil>=5.9.0

# Camera  ← NEW
opencv-python>=4.8.0

# HTTP / Weather / News
requests>=2.31.0

# Screenshot
pyautogui>=0.9.54
Pillow>=10.0.0

# Audio metering (waveform HUD)
sounddevice>=0.4.6

# Clipboard
pyperclip>=1.8.2

# Offline wake word + offline speech recognition + WhatsApp
numpy>=1.26.0
vosk>=0.3.45
openwakeword>=0.6.0
onnxruntime>=1.17.0
pywhatkit>=5.4

# Face recognition (requires CMake + C++ build tools on Windows for dlib)
face_recognition>=1.3.0
dlib>=19.24.0

# Voice affect / prosody features (mood heuristics)
librosa>=0.10.0
soundfile>=0.12.0
# FastAPI CoWork dependencies
fastapi>=0.95.2
uvicorn[standard]>=0.22.0
python-jose>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
sqlalchemy>=2.0.0
cryptography>=41.0.0
pytest
